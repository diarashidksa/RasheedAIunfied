# Pin NumPy <2 for FAISS compatibility
numpy<2

# FAISS CPU version (switch to faiss-gpu if you have GPU)
faiss-cpu

# Web framework
flask==2.3.2

# WSGI server
gunicorn==21.2.0

# Other dependencies (add yours)
requests==2.31.0
python-dotenv==1.0.0


pandas

